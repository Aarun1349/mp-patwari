import type { NextConfig } from "next";

// Baseline security headers for production. (A full Content-Security-Policy is
// intentionally omitted here — it needs an allowlist for Razorpay checkout and
// Google OAuth, so it gets its own pass before launch rather than a broken one
// that blocks payments.)
const securityHeaders = [
  { key: "X-Frame-Options", value: "DENY" },
  { key: "X-Content-Type-Options", value: "nosniff" },
  { key: "Referrer-Policy", value: "strict-origin-when-cross-origin" },
  { key: "Strict-Transport-Security", value: "max-age=63072000; includeSubDomains; preload" },
  { key: "Permissions-Policy", value: "camera=(), microphone=(), geolocation=()" },
];

const nextConfig: NextConfig = {
  async headers() {
    return [{ source: "/:path*", headers: securityHeaders }];
  },
};

export default nextConfig;
