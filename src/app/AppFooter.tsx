import { AppInfoLinks } from "./AppInfoLinks";

export function AppFooter() {
  return (
    <footer className="app-footer">
      ExamsExpress — an independent practice platform, not affiliated with any exam board.
      <AppInfoLinks />
    </footer>
  );
}
